# Agent Notes

- Never run `npm publish` or otherwise publish a package version.
- Prepare and verify the release locally, then ask the user to perform the
  publication and provide the exact command.
